# JobsUPI Seeker UI

Vite + React + TypeScript + Tailwind front-end that consumes the agent and core APIs.

## Scripts
- `npm run dev`
- `npm run build`
- `npm run preview`
- `npm run lint`
- `npm run test` (Vitest)
- `npm run jest`

Set environment variables in `.env` based on `.env.example`.
